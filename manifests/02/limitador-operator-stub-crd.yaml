apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: limitadoroperators.limitador.kuadrant.io
spec:
  group: limitador.kuadrant.io
  names:
    kind: LimitadorOperator
    listKind: LimitadorOperatorList
    plural: limitadoroperators
    singular: limitadoroperator
  scope: Cluster
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        x-kubernetes-preserve-unknown-fields: true
